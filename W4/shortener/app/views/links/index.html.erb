<h1>Gimme the link!</h1>

<form action="/newlink" method="POST">
  <div>
    <label for="original_url"></label>
    <input type="text" name="original_url" placeholder="Paste your url here">
    <button type="submit">GO</button>
  </div>
  <input type="hidden" name="authenticity_token"
      value="<%= form_authenticity_token %>">
</form>


<h2>Latest links shortened</h2>

<% @links.each do |link| %>
  <p>The teeny tiny url for "<%= link.original_url %>" is: <%= link.short_url %></p>
<% end %>
